import { Box, Container, Heading, Text } from '@chakra-ui/react';

export const NewArrivalsPage = () => {
  return (
    <Container maxW="container.xl" py={8}>
      <Heading mb={4}>New Arrivals</Heading>
      <Text>Check out our latest products.</Text>
    </Container>
  );
};
